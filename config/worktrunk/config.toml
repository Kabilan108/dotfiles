worktree-path = ".worktrees/{{ branch | sanitize }}"

[commit.generation]
command = "llm -m openrouter/openai/gpt-5-nano"

[post-switch]
tmux-pane = "[ -n \"$TMUX\" ] && tmux select-pane -T {{ branch | sanitize }}"

[pre-merge]
notify = "notify-send 'Merging {{ branch }}'"
