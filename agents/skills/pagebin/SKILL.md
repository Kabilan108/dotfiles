---
name: pagebin
description: Publish and manage protected artifacts with the PageBin CLI. Use when asked to publish, update, watch, verify, recover, list, reissue, expire, or delete a PageBin artifact independently of an authoring skill.
---

# PageBin

Load the installed CLI's version-matched instructions before acting:

```sh
pagebin skill
```

Follow those instructions as the source of truth. Never publish credentials or secrets, expose viewer URLs unnecessarily, or commit PageBin receipt data.
