# Codex-only skills

Each child directory is symlinked into `agents/codex/skills` by `sync-agent-skills`.
Hidden directories such as `.system` are allowed here for Codex-specific skill roots.
The generated `.system` bundle is ignored by git.
